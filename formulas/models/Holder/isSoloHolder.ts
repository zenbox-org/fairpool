import { Address } from '../../../../ethereum/models/Address'
import { todo } from '../../../../utils/todo'
import { Fairpool } from '../Fairpool'

export const isSoloHolder = (fairpool: Fairpool) => (address: Address) => todo()
