import { Address } from '../../../../ethereum/models/Address'
import { todo } from '../../../../utils/todo'
import { Fairpool } from './index'

export const hasExternalShares = (fairpool: Fairpool) => (sender: Address) => todo()
